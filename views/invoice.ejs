<!DOCTYPE html>
<html>
<head>
    <title><%= t.invoice_title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <style>
        * { box-sizing: border-box; }
        body { font-family: sans-serif; padding: 20px; background: #eee; min-height: 100vh; display: flex; flex-direction: column; align-items: center; }
        h2 { text-align: center; }
        .invoice-box { background: white; width: 100%; max-width: 500px; padding: 25px; border: 1px solid #ddd; box-shadow: 0 4px 10px rgba(0,0,0,0.1); border-radius: 8px; position: relative; }
        .row { display: flex; justify-content: space-between; margin-bottom: 12px; border-bottom: 1px solid #eee; padding-bottom: 8px; font-size: 14px; }
        .total { font-size: 1.1em; font-weight: bold; color: #333; border-top: 2px solid #333; padding-top: 15px; margin-top: 10px; }
        .status { font-weight: bold; padding: 4px 8px; border-radius: 4px; color: white; font-size: 12px; }
        .Pending { background: orange; } .Approved { background: green; } .Rejected { background: red; }
        .pay-btn { display: block; background: #28787e; color: white; text-align: center; padding: 15px; text-decoration: none; border-radius: 5px; margin-top: 20px; font-weight: bold; font-size: 16px; }
        .pay-btn:hover { background: #63e6ef; }
        .note { color: #d9534f; font-size: 0.9em; margin-top: 20px; text-align: center; background: #fff3f3; padding: 10px; border-radius: 4px; border: 1px solid #ffcccc; }
        a.back { display: block; margin-top: 20px; text-align: center; color: #666; text-decoration: none; padding: 10px; }
        details { margin-top: 15px; text-align: left; border-top: 1px solid #ffcccc; padding-top: 10px; }
        summary { cursor: pointer; color: #0e4f53; font-weight: bold; list-style: none; text-align: center; }
        summary::-webkit-details-marker { display: none; }
        .vat-info { font-size: 0.85em; color: #333; margin-top: 10px; line-height: 1.5; text-align: left; }
        .vat-info p { margin-bottom: 10px; }
        .lang-menu { position: absolute; top: 10px; right: 10px; display: inline-block; }
        .lang-btn { background: none; border: none; font-size: 24px; cursor: pointer; color: #333; }
        .lang-dropdown { display: none; position: absolute; right: 0; background-color: white; min-width: 120px; box-shadow: 0 8px 16px rgba(0,0,0,0.2); z-index: 1000; border-radius: 8px; overflow: hidden; }
        .lang-dropdown a { color: black; padding: 12px 16px; text-decoration: none; display: block; font-size: 14px; text-align: left; }
        .lang-dropdown a:hover { background-color: #f1f1f1; }
        .lang-menu:hover .lang-dropdown { display: block; }
        @media (max-width: 400px) { .row { flex-direction: column; gap: 5px; } .row span:nth-child(2) { font-weight: bold; } }
    </style>
</head>
<body>
    <h2><%= t.invoice_title %></h2>
    <div class="invoice-box">
        <div class="lang-menu">
            <button class="lang-btn"><i class='bx bx-globe'></i></button>
            <div class="lang-dropdown">
                <a href="/set-lang/en">🇺🇸 English</a>
                <a href="/set-lang/cs">🇨🇿 Čeština</a>
                <a href="/set-lang/hr">🇭🇷 Hrvatski</a>
                <a href="/set-lang/hu">🇭🇺 Magyar</a>
            </div>
        </div>
        <h3 style="margin-top:0; text-align:center;"><%= t.invoice_num %><%= tx._id.toString().slice(-6).toUpperCase() %></h3>
        <div class="row"><span><%= t.user_label %></span> <span><%= tx.userId.username %></span></div>
        <div class="row"><span><%= t.status_label %></span> <span class="status <%= tx.status %>"><%= (tx.status === 'Approved' ? t.approved : (tx.status === 'Pending' ? t.pending : t.rejected)) %></span></div>
        <div class="row"><span><%= t.amount_label %></span> <span>€<%= tx.amount %></span></div>
        <div class="row"><span><%= t.method_label %></span> <span><%= tx.method %></span></div>
        <div class="row"><span><%= t.dest_label %></span> <span style="word-break: break-all;"><%= tx.details %></span></div>
        <% if (tx.status === 'Approved') { %>
            <div class="row" style="color:green; font-weight:bold;"><span><%= t.vat_fee %></span> <span>€<%= tx.vatFee %> <%= t.paid %></span></div>
        <% } else { %>
            <div class="row" style="color:red;"><span><%= t.vat_fee %></span> <span>€<%= tx.vatFee %> <%= t.pending %></span></div>
        <% } %>
        <div class="row total"><span><%= t.total_withdraw %></span> <span>€<%= tx.amount %></span></div>
        <% if (tx.status === 'Pending') { %>
            <div class="note">
                <strong><%= t.action_required %></strong><br>
                <%= t.pay_vat_note %> <strong>€<%= tx.vatFee %></strong> <%= t.pay_vat_note_2 %>
                <details>
                    <summary><%= t.click_vat_info %></summary>
                    <div class="vat-info">
                        <p><%= t.vat_p1 %></p><p><%= t.vat_p2 %></p><p><%= t.vat_p3 %></p>
                        <p><strong><%= t.vat_note_header %></strong> <%= t.vat_note_body %></p>
                        <p><strong><%= t.vat_terms %></strong><br><%= t.vat_terms_body %></p>
                        <p style="color: red; font-weight: bold; text-align: center;"><%= t.vat_disclaimer %></p>
                    </div>
                </details>
            </div>
        <% } %>
    </div>
    <a href="/dashboard" class="back"><%= t.back_wallet %></a>
</body>
</html>