<!DOCTYPE html>
<html>
  <head>
    <title><%= t.withdraw %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <style>
      * {
        box-sizing: border-box;
      }
      body {
        font-family: sans-serif;
        padding: 20px;
        background: #f8f9fa;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .card {
        background: white;
        padding: 30px;
        border-radius: 8px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 500px;
        position: relative;
      }
      h2 {
        text-align: center;
        margin-top: 0;
      }
      input,
      select {
        width: 100%;
        padding: 15px;
        margin: 10px 0;
        box-sizing: border-box;
        border: 1px solid #ccc;
        border-radius: 4px;
        font-size: 16px;
        background: #fff;
      }
      button {
        width: 100%;
        padding: 15px;
        background: #00b894;
        color: white;
        border: none;
        cursor: pointer;
        border-radius: 4px;
        font-size: 18px;
        margin-top: 15px;
        font-weight: bold;
      }
      label {
        font-weight: bold;
        margin-top: 15px;
        display: block;
      }
      a {
        display: block;
        text-align: center;
        margin-top: 20px;
        color: #666;
        text-decoration: none;
        padding: 10px;
      }
      .lang-menu {
        position: absolute;
        top: 10px;
        right: 10px;
        display: inline-block;
      }
      .lang-btn {
        background: none;
        border: none;
        font-size: 24px;
        cursor: pointer;
        color: #333;
      }
      .lang-dropdown {
        display: none;
        position: absolute;
        right: 0;
        background-color: white;
        min-width: 120px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        z-index: 1000;
        border-radius: 8px;
        overflow: hidden;
      }
      .lang-dropdown a {
        color: black;
        padding: 12px 16px;
        text-decoration: none;
        display: block;
        font-size: 14px;
        text-align: left;
      }
      .lang-dropdown a:hover {
        background-color: #f1f1f1;
      }
      .lang-menu:hover .lang-dropdown {
        display: block;
      }
    </style>
  </head>
  <body>
    <div class="card">
      <div class="lang-menu">
        <button class="lang-btn"><i class="bx bx-globe"></i></button>
        <div class="lang-dropdown">
          <a href="/set-lang/en">🇺🇸 English</a>
          <a href="/set-lang/cs">🇨🇿 Čeština</a>
          <a href="/set-lang/hr">🇭🇷 Hrvatski</a>
          <a href="/set-lang/hu">🇭🇺 Magyar</a>
        </div>
      </div>
      <h2><%= t.withdraw_rewards %></h2>
      <form action="/withdraw/confirm" method="POST">
        <label><%= t.amount_eur %></label>
        <input
          type="number"
          step="0.01"
          name="amount"
          placeholder="0.00"
          required
        />
        <label><%= t.payment_method %></label>
        <select name="method" required>
          <option value="Bank Transfer"><%= t.bank_transfer %></option>
          <option value="Crypto"><%= t.crypto_payment %></option>
        </select>
        <label><%= t.acc_wallet %></label>
        <input
          type="text"
          name="details"
          placeholder="<%= t.enter_details %>"
          required
        />
        <button type="submit"><%= t.process_withdraw %></button>
      </form>
      <a href="/dashboard"><%= t.cancel %></a>
    </div>
  </body>
</html>
