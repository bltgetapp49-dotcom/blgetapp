<!DOCTYPE html>
<html>
<head>
    <title><%= t.login %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <style>
        * { box-sizing: border-box; }
        body { font-family: sans-serif; background: #0f2027; display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; padding: 20px; }
        .box { background: white; padding: 30px; border-radius: 8px; box-shadow: 0 4px 10px rgba(0,0,0,0.1); text-align: center; width: 100%; max-width: 400px; position: relative; }
        img { width: 80px; margin-bottom: 15px; }
        input { width: 100%; padding: 12px; margin: 10px 0; border: 1px solid #ddd; border-radius: 4px; font-size: 16px; }
        button { width: 100%; padding: 12px; background: #28787e; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 16px; font-weight: bold; margin-top: 10px; }
        button:hover { background: #63e6ef; }
        .link { margin-top: 20px; font-size: 14px; }
        .error { color: red; font-size: 14px; }
        .lang-menu { position: absolute; top: 10px; right: 10px; display: inline-block; }
        .lang-btn { background: none; border: none; font-size: 24px; cursor: pointer; color: #333; }
        .lang-dropdown { display: none; position: absolute; right: 0; background-color: white; min-width: 120px; box-shadow: 0 8px 16px rgba(0,0,0,0.2); z-index: 1000; border-radius: 8px; overflow: hidden; }
        .lang-dropdown a { color: black; padding: 12px 16px; text-decoration: none; display: block; font-size: 14px; text-align: left; }
        .lang-dropdown a:hover { background-color: #f1f1f1; }
        .lang-menu:hover .lang-dropdown { display: block; }
    </style>
</head>
<body>
    <div class="box">
        <div class="lang-menu">
            <button class="lang-btn"><i class='bx bx-globe'></i></button>
            <div class="lang-dropdown">
                <a href="/set-lang/en">🇺🇸 English</a>
                <a href="/set-lang/cs">🇨🇿 Čeština</a>
                <a href="/set-lang/hr">🇭🇷 Hrvatski</a>
                <a href="/set-lang/hu">🇭🇺 Magyar</a>
            </div>
        </div>
        <img src="/images/logo.png" alt="Logo">
        <h2><%= t.welcome_back %></h2>
        <% if (locals.error) { %> <p class="error"><%= error %></p> <% } %>
        <form action="/login" method="POST">
            <input type="text" name="username" placeholder="<%= t.username %>" required>
            <input type="password" name="password" placeholder="<%= t.password %>" required>
            <button type="submit"><%= t.login %></button>
        </form>
        <div class="link">
            <%= t.dont_have %> <br>
            <a href="/register" style="color:#0e4f53; text-decoration:none;"><%= t.create_account %></a>
        </div>
    </div>
</body>
</html>